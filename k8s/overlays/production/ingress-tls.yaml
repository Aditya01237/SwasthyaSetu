apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swasthya-ingress
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
spec:
  tls:
    - secretName: swasthya-tls
