server:
  port: 8085
spring:
  application:
    name: appointment-service
  datasource:
    url: jdbc:postgresql://localhost:5432/swasthya_appointment
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
  rabbitmq:
    host: localhost
    port: 5672
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
